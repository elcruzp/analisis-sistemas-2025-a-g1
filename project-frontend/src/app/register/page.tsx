'use client';

import UserForm from '@/modules/componentes/userForm';

export default function RegisterPage() {
  return (
    <main className="flex items-center justify-center h-screen bg-gray-100">
      <UserForm />
    </main>
  );
}